const randomInteger = require("./3-container-for-exports");

console.log(randomInteger()); // 97
console.log(randomInteger()); // 30
console.log(randomInteger()); // 29


/*
we can make modules of our own and call it inside the file we want them to use that module
in node js everything is a module.
 */
